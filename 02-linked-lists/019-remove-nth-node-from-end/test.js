const assert = require('assert');
// Placeholder test for Remove Nth Node From End

describe('019 Remove Nth Node From End', () => {
  it('example', () => {
    assert.strictEqual(true, true);
  });
});
