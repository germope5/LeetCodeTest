/* eslint-env node */
const assert = require('assert');
const { describe, it } = require('node:test');

describe('001 Group Anagrams', () => {
  it('placeholder', () => {
    assert.strictEqual(true, true);
  });
});
