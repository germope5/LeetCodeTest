const assert = require('assert');
// Placeholder tests for binary tree level order

describe('102 Binary Tree Level Order', () => {
  it('sample', () => {
    assert.strictEqual(true, true);
  });
});
